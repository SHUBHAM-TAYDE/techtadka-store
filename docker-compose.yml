version: '3.8'
services:
  techtadka-store:
    build:
      context: .
    ports:
      - "80:80"
    restart: always
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      retries: 3
